apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-my-app
  labels:
    app: my-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: my-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
